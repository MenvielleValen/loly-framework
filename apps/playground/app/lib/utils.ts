/**
 * Utils module for dynamic imports example
 */
export const utils = {
  formatDate: (date: Date) => date.toLocaleDateString(),
  formatTime: (date: Date) => date.toLocaleTimeString(),
};

export default utils;


