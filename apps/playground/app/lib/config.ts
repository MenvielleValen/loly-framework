/**
 * Config module for dynamic imports example
 */
export const config = {
  appName: "Loly Playground",
  version: "1.0.0",
  env: process.env.NODE_ENV || "development",
};

export default config;

